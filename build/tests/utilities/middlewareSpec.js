"use strict";
/*import { validation,resize } from "../../utilities/middleware";
import { NextFunction, Request, Response } from 'express';*/
/*import app from '../../index';
import supertest from 'supertest';

const request = supertest(app);*/
/*const req={
    image:'image2',
    width:'200',
    height:'200'
};
const res={};
const next=function(){console.log}
describe("Testing the resizing MiddleWare",():void=>{

    it("file path must be exist ",async()=>{

        const result =await resize(req,res,next);
        
        expect(result.Response.status).toBe(200);
    })
});*/
/*
describe("Testing the Query String",():void=>{

    it("Query string should be not empty",async()=>{
        const result =await validation(mockRequest,mockResponse,next);

})
});*/ 
